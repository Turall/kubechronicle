import { AppRoutes } from './routes';
import './App.css'; // Might remove this if interferes with Tailwind, but leaving for now as I need to check its content.

function App() {
  return <AppRoutes />;
}

export default App;
