# Example: Manually created TLS secret
# This is an example of what the secret should look like.
# In practice, use ./generate-certs.sh or make generate-certs to create it automatically.
#
# To create manually:
#   1. Generate certificates using openssl (see generate-certs.sh)
#   2. Create secret: kubectl create secret tls kubechronicle-webhook-tls --cert=tls.crt --key=tls.key -n kubechronicle

apiVersion: v1
kind: Secret
metadata:
  name: kubechronicle-webhook-tls
  namespace: kubechronicle
type: kubernetes.io/tls
data:
  # Base64 encoded certificate and key
  # tls.crt: <base64-encoded-certificate>
  # tls.key: <base64-encoded-private-key>
  #
  # DO NOT commit actual certificates to git!
  # Use ./generate-certs.sh to generate and create the secret automatically.
